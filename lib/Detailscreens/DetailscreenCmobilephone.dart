import 'package:flutter/material.dart';
class DetailscreenCmobilephone extends StatelessWidget {
  const DetailscreenCmobilephone({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
